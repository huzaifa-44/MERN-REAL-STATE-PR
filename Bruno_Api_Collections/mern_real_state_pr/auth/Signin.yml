info:
  name: Signin
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/auth/signin
  body:
    type: json
    data: |
      {
        "email": "tested@gmail.com",
        "password": "tested"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
