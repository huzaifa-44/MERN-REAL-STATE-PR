info:
  name: Signup
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/auth/signup
  body:
    type: json
    data: |
      {
        "username": "tested",
        "email": "tested@gmail.com",
        "password": "tested"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
